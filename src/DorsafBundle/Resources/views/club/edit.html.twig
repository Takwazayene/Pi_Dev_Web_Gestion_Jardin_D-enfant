{% extends 'base1.html.twig' %}

{% block body %}
<section class="breadcrumb_part">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="breadcrumb_iner">
                    <h2>Espace event</h2>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- breadcrumb part end-->

<div class="container" style="margin-top:100px;">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header card-header-primary">
                <h4 class="card-title">Event</h4>
                <p class="card-category">Merci de modifier les informations</p>
            </div>
            <div class="card-body">
                <h1>Club edit</h1>

                {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <input type="submit" value="Edit" class="genric-btn primary circle" />
                {{ form_end(edit_form) }}

                <ul>
                    <li>
                        <a class="genric-btn primary circle" href="{{ path('club_index') }}">Back to the list</a>
                    </li>
                    <li>
                        {{ form_start(delete_form) }}
                        <input type="submit" value="Delete" class="genric-btn primary circle" >
                        {{ form_end(delete_form) }}
                    </li>
                </ul>
                {% endblock %}
